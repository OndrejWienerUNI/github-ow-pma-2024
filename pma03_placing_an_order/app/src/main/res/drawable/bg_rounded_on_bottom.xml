<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- White background -->
    <corners android:bottomLeftRadius="12dp" android:bottomRightRadius="12dp"/> <!-- Only bottom corners rounded -->
    <stroke android:width="0dp" android:color="@color/light_gray"/> <!-- Optional border -->
</shape>
